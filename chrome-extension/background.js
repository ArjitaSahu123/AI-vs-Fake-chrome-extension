// background.js can be used to set up any background processing or handling multiple tab states
chrome.runtime.onInstalled.addListener(() => {
  console.log('Image Real/Fake Detector Extension Installed!');
});
